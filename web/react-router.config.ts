import type { Config } from "@react-router/dev/config";

export default {
  // Server-side render by default (proper SSR); routes can opt into client-only where needed.
  ssr: true,
} satisfies Config;
